version: '3.8'

services:
  ollama:
    image: ollama/ollama:latest
    container_name: private_llm_engine
    restart: unless-stopped
    # 100% isolated. No egress (outbound) internet access due to internal network.
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - ai_secure_net
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  nginx:
    image: nginx:alpine
    container_name: ai_proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    networks:
      - ai_secure_net
      - public_proxy_net # Nginx connects to both the world and the isolated AI
    depends_on:
      - ollama

networks:
  ai_secure_net:
    internal: true 
  public_proxy_net:
    driver: bridge

volumes:
  ollama_data:
